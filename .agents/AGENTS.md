# Project Rules (專案協作規範)

## Timestamp Rule for Logs
- Whenever a log entry or log file is written or updated (e.g., in `log.md`), always include a TIMESTAMP (Format: `YYYY-MM-DD HH:MM:SS`) on the first row / header of each log entry.
- Ensure all team members and AI tools follow this rule for log updates.
